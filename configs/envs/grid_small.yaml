# Environment: Small grid (baseline replication)
map_size: [5, 6, 7, 8, 9]
num_uavs: [1, 2, 3]
obstacle_density: 0.0  # Start with no obstacles for baseline
obstacle_type: static
max_steps: 1000
energy_budget: 1800  # 30 minutes * 60 seconds
shaping_weight: 10.0
obstacle_shaping_weight: 3.5
