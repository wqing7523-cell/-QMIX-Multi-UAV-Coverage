algorithm: qmix
learning_rate: 0.0005
gamma: 0.99
buffer_size: 5000
batch_size: 32
min_buffer: 200
agent_hidden_dim: 64
mixing_hidden_dim: 32
hyper_hidden_dim: 64
epsilon_start: 1.0
epsilon_end: 0.05
epsilon_decay: 0.995
target_update_interval: 200
episodes: 100
log_interval: 10
double_q: false
grad_clip: 10.0
